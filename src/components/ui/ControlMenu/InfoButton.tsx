// InfoButton.tsx
import {Button} from "../button";

const InfoButton = () => {
  return (
    <>
      <Button
        onClick={() => { }}
        color='secondary'
      >
        Wander
      </Button>
    </>
  );
};

export default InfoButton;
