// HelpButton.tsx
import {Button} from "../button";

const HelpButton = () => {
  return (
    <>
      <Button
        onClick={() => { }}
        color='secondary'
      >
        Help Button
      </Button>
    </>
  );
};

export default HelpButton;
